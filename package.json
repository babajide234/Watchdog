{
  "name": "react-watchdog",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm run --parallel --filter './packages/**' --filter './apps/**' dev",
    "build": "pnpm run --parallel --filter './packages/**' --filter './apps/**' build",
    "lint": "pnpm run --recursive lint",
    "clean": "pnpm run --recursive clean"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
